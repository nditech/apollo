# Apollo Terraform

Terraform configuration for Apollo AWS infrastructure.

## Current scope

This currently manages:
- S3 bucket for attachments

## Usage

From this directory:

```bash
terraform init
terraform plan
terraform apply
