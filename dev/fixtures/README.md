These files are local/dev import fixtures for manually testing Apollo imports: locations, participants, and forms

Suggested conventions:
- `*-valid.*` = known-good import files
- `*-invalid.*` = intentionally broken files for validation/error-path testing
- keep files small and easy to inspect by eye
- avoid real or sensitive data