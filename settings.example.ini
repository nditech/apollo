; Local Apollo application settings.
; Copy this file to settings.ini and replace placeholder values as needed.
; Do not commit real secrets in settings.ini.
; SECRET_KEY is required for Apollo to start correctly.
[settings]
SECRET_KEY=local-dev-password
PREFERRED_URL_SCHEME=http
TIMEZONE=America/New_York
